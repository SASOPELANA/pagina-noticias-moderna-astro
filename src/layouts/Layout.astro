---
import "../styles/global.css";
import Navbar from "../components/Navbar.astro";
import Footer from "../components/Footer.astro";
import favicom from "../assets/icons/news-updates-icon.png";

// Interface Astro
interface Props {
  title: string;
  description: string;
}

// Creamos una objeto que recibe las props --> Astro.props
const { title, description } = Astro.props;
---

<!doctype html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <meta
      name="description"
      content={description ?? "Noticias modernas y actualizadas."}
    />
    <link rel="icon" type="image/svg+xml" href={favicom.src} />
    <meta name="generator" content={Astro.generator} />
    <title>{title || "Pagina de Noticias Moderna."}</title>
  </head>
  <body class="flex flex-col items-center font-Inter">
    <main class="w-full xl:w-[1112px] px-2 my-8">
      <Navbar />
      <slot />
      <Footer />
    </main>
  </body>
</html>

<style>
  html,
  body {
    margin: 0;
    width: 100%;
    height: 100%;
  }
</style>
